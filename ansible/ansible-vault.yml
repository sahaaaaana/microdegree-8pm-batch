---
- name: ansible palybook to test ansible vault
  hosts: all
  become: true
  vars_files:
    - vault-pass.yml
  tasks:
  - name: clone a repo
    git: 
      repo: https://github.com/ManojKRISHNAPPA/microdegree-8pm-batch.git
      dest: /opt/ansadmin/test-vault
